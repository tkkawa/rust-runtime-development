# rust-runtime

## Install Container Runtime
### Build the Docker Container for container runtime
```
$ make build_runtime
$ make run_runtime
$ make exec_runtime
```

### Build container runtime
```
$ cargo build
$ make install
```
